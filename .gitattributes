# Normalize line endings to LF in the repo (Docker images build on Linux).
* text=auto eol=lf

# Treat these as binary — never normalize.
*.png binary
*.jpg binary
*.jpeg binary
*.pdf binary
*.ico binary
*.svg text
